const mongoose = require("mongoose");

require("../mongodb_helper");
const Bookstore = require("../../models/bookstores");


describe('bookstores collection', () => {
  it('returns the names of two bookshops', () => {
    
  })
})